{
  "if": {
      "prefix": "if",
      "body": [
          "{{#if ${1:condition}}}",
          "  $2",
          "{{/if}}"
      ],
      "description": "if"
  },
  "else": {
      "prefix": "else",
      "body": [
          "{{else}}",
          "  $1"
      ],
      "description": "else"
  },
  "ifelse": {
      "prefix": "ifelse",
      "body": [
        "{{#if ${1:condition}}}",
        "  $2",
        "{{else}}",
        "  $3",
        "{{/if}}"
      ],
      "description": "elsif"
  },
  "unless": {
      "prefix": "unless",
      "body": [
          "{{#unless ${1:condition}}}",
          "  $2",
          "{{/unless}}"
      ],
      "description": "unless"
  },
  "each": {
      "prefix": "each",
      "body": [
          "{{#each ${1}}",
          "  $2",
          "{{/each}}"
      ],
      "description": "each"
  },
  "with": {
      "prefix": "with",
      "body": [
          "{{#with ${1}}",
          "  $2",
          "{{/with}}"
      ],
      "description": "with"
  }
}
